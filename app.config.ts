import { ExpoConfig } from "expo/config";

const config: ExpoConfig = {
  name: "react-native-expo-boilerplate",
  slug: "react-native-expo-boilerplate",
  scheme: "react-native-expo-boilerplate",
};

export default config;
